{
  "name": "postgres-sink",

  "config": {
    "connector.class": "io.debezium.connector.jdbc.JdbcSinkConnector",

    "tasks.max": "1",

    "topics": "postgres_source.public.test_cdc",

    "connection.url": "jdbc:postgresql://postgres_target:5432/analytics_target",

    "connection.username": "airflow_user",
    "connection.password": "super_secure_password123",

    "insert.mode": "insert",

    "auto.create": "true",

    "auto.evolve": "true"
  }
}